# 审计流水线可视化就绪状态

## 当前情况

Jenkins 的阶段可视化依赖于流水线工作流插件。在创建任务之前，请确认该虚拟机已安装所需的流水线工作流插件。

## 范围

- Jenkins 运行在名为 `jenkins` 的 Docker 容器中。
- 插件文件位于 `/var/jenkins_home/plugins` 下。
- 将审计结果写入 `/home/labex/project/pipeline-plugin-audit.txt`。

## 你的目标

创建一个报告，证明 Jenkins 实例具备流水线工作流插件支持。

## 验收标准

- 报告文件存在于 `/home/labex/project/pipeline-plugin-audit.txt`。
- 报告中包含 `workflow-job.jpi`。
- 报告中至少包含另一个 `workflow-*.jpi` 插件文件。

## 提示

<details>
<summary>需要一个起点吗？</summary>

在 Jenkins 容器内列出 `/var/jenkins_home/plugins` 并过滤 `workflow`。
</details>
