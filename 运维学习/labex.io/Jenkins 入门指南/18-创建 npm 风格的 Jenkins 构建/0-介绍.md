# 简介

Jenkins 经常被用于自动化 npm 风格的 JavaScript 工作流：项目包含 `package.json`，依赖安装会创建 `node_modules`，测试会生成报告，而打包则会产生可分发的制品。由于本实验环境的虚拟机不包含 NodeJS/npm Jenkins 插件或 Node 运行时，因此本挑战将使用确定性的 shell 步骤来练习 Jenkins 工作流的构建方式，而无需进行外部下载。

你将创建自由风格（Freestyle）和流水线（Pipeline）任务，以生成 npm 风格的项目文件、报告和制品。
