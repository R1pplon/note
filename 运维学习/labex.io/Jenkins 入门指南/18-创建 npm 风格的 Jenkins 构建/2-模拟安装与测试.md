# 模拟安装与测试

## 当前情况

项目清单已经存在。请扩展该任务，使其模拟依赖安装和测试运行。

## 范围

- 继续使用 `npm-style-project` 任务。
- 创建 `node_modules/.installed`。
- 创建 `reports/test-result.txt`。
- 再次运行该任务。

## 目标

更新并运行任务，使工作区包含安装和测试的凭证。

## 验收标准

- 任务配置创建了 `node_modules/.installed`。
- 工作区包含 `reports/test-result.txt`。
- 最近的构建日志包含 `npm-style tests passed`。

## 提示

<details>
<summary>需要一个起点吗？</summary>

替换 shell 构建步骤，使其能够写入清单、创建目录并写入测试报告。
</details>
