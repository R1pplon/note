# 简介

Jenkins 通常用于协调 Maven 风格的 Java 构建：项目包含一个 `pom.xml`，构建输出位于 `target/` 目录下，并且测试报告会被归档以供检查。由于本实验环境的虚拟机中不包含 Maven 插件或 Maven 二进制文件，因此本次挑战将重点关注 Jenkins 中一种稳定的集成模式：即任务能够生成符合 Maven 规范的项目文件、构建产物和报告。

你将创建一个自由风格任务和一个流水线任务，在确定性的离线环境中模拟 Maven 构建生命周期。
