# 总结

在本实验中，你检查了一个基于 Docker 运行的 Jenkins 部署。

你练习了以下操作：

- 确认 Docker 可用。
- 识别正在运行的 Jenkins 容器。
- 检查 Jenkins Docker 镜像。
- 检查端口 `8080` 的暴露情况。
- 检查 `/var/jenkins_home`。
- 读取 Jenkins 容器日志。
- 打开 Jenkins Web 仪表板。

这些技能有助于你理解和排查基于 Docker 的 Jenkins 环境，而无需从头开始重建控制器。
