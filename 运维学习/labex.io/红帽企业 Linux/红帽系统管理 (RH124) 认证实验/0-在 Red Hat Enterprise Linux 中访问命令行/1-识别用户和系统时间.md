# 识别用户和系统时间

在本步骤中，你将学习如何使用基本的 Linux 命令来识别当前登录系统的用户以及如何显示系统的当前日期和时间。理解这些基本命令对于在任何 Linux 环境中进行导航和管理至关重要。

首先，让我们找出你当前登录的用户。`whoami` 命令用于此目的。它会打印出当前用户的有效用户名。

在你的终端中输入以下命令：

```bash
whoami
```

![Screenshot of whoami command output](https://file.labex.io/namespace/df87b950-1f37-4316-bc07-6537a1f2c481/rhel/rh124/access-command-line-in-red-hat-enterprise-linux/zh/../assets/20250604-10-33-42-EC8EfwfR.png)

你应该会看到 `labex` 作为输出，这表明你以 `labex` 用户身份登录。

```plaintext
labex
```

接下来，我们将探索 `date` 命令。此命令用于显示或设置系统日期和时间。当不带任何选项使用时，它会以默认格式显示当前的日期和时间。

执行 `date` 命令：

```bash
date
```

输出将显示当前的星期几、月份、日期、时间（HH:MM:SS）、时区和年份。确切的输出将根据你运行命令的时间而有所不同。

```plaintext
Mon Jul 22 10:30:45 AM UTC 2024
```

`date` 命令非常灵活，允许你使用格式说明符以各种方式格式化输出。例如，要仅以 24 小时制（HH:MM）显示当前时间，你可以使用 `+%R`。

尝试仅显示时间：

```bash
date +%R
```

输出将类似于：

```plaintext
10:30
```

要仅以 MM/DD/YYYY 格式显示当前日期，你可以使用 `+%x`。

尝试仅显示日期：

```bash
date +%x
```

输出将类似于：

```plaintext
07/22/2024
```

最后，你可以通过分号（`;`）分隔在一行上执行多个命令。这对于快速运行一系列命令非常有用。

让我们尝试在同一行上运行 `whoami` 和 `date`：

```plaintext
whoami; date
```

你将看到两个命令的输出，一个接一个：

```plaintext
labex
Mon Jul 22 10:30:45 AM UTC 2024
```

![Screenshot of whoami and date commands output](https://file.labex.io/namespace/df87b950-1f37-4316-bc07-6537a1f2c481/rhel/rh124/access-command-line-in-red-hat-enterprise-linux/zh/../assets/20250613-15-35-06-cCRA36BP.png)
