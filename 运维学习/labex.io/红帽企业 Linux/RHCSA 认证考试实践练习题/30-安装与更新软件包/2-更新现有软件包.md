# 更新现有软件包

保持软件处于最新状态对于安全性和稳定性至关重要。你的下一个任务是将现有软件包 `vim` 更新到仓库中可用的最新版本。

## 任务

- 更新 `vim` 软件包。

## 要求

- 使用 `dnf` 命令执行更新。
- 你必须拥有 `sudo` 权限才能更新软件。

## 示例

更新 `vim` 软件包后，你可以检查其版本。输出应显示新的、已更新的版本号。

```bash
vim --version
```

```plaintext
VIM - Vi IMproved 9.x
...
```
