# 移除单个容器

在这一步中，你将学习如何使用 `docker rm` 命令移除单个 Docker 容器。幽灵导师将引导你完成这一过程。

#### 示例代码

首先，运行以下命令创建名为 `mycontainer` 的容器：

```zsh
docker run --name mycontainer alpine:latest
```

运行以下命令移除名为 `mycontainer` 的容器：

```zsh
docker rm mycontainer
```
