# 拉取多个镜像

在此步骤中，你将从仓库拉取多个 Docker 镜像，并检查它们的详细信息。

## 任务

- 从官方仓库拉取 `alpine` 和 `ubuntu` Docker 镜像。
- 列出已拉取镜像的详细信息，包括它们的标签（tag）和大小。

## 要求

- 使用 Docker 命令行界面拉取 `alpine` 和 `ubuntu` 镜像。
- 验证拉取的镜像是否存在于本地 Docker 仓库中。

## 示例

执行 Docker pull 命令后，终端应显示有关已拉取镜像的信息，包括它们的标签和大小。
