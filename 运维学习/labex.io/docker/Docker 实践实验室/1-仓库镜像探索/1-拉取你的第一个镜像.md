# 拉取你的第一个镜像

在此步骤中，你的任务是从仓库拉取一个特定的 Docker 镜像。

## 任务

- 从官方仓库拉取 `nginx` Docker 镜像。
- 验证镜像是否成功下载。

## 要求

- 使用 Docker 命令行界面拉取 `nginx` 镜像。
- 在 zsh 终端中使用默认工作路径：`~/project`。

## 示例

执行 Docker pull 命令后，控制台应显示镜像下载的进度和最终成功的确认信息。
