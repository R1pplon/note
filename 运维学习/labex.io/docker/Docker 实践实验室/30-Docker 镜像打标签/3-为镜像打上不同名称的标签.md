# 为镜像打上不同名称的标签

在这一步中，你将为一个现有镜像打上不同的名称和标签，以便进行实验和版本控制。

1. 为 "alpine" 镜像打上不同的名称和标签：

   ```bash
   docker pull alpine
   docker tag alpine myalpine:v1.0
   ```

2. 验证已打标签的镜像：

   ```bash
   docker images
   ```
