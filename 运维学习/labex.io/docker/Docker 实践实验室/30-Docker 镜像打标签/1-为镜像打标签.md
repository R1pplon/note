# 为镜像打标签

在这一步中，你将学习如何为 Docker 镜像打标签，以提供自定义的名称和标签。首先，确保你的 Linux 环境中已安装 Docker 版本 20.10.21。

1. 为 "hello-world" 镜像打上自定义标签：

   ```bash
   docker pull hello-world
   docker tag hello-world myhello:latest
   ```

2. 验证已打标签的镜像：

   ```bash
   docker images
   ```
