# 为镜像打上注册表地址标签

在这一步中，你将为一个 Docker 镜像打上注册表地址标签，以便将来将其部署到特定的仓库中。

1. 为 "nginx" 镜像打上注册表地址标签：

   ```bash
   docker pull nginx
   docker tag nginx myregistry.com/mynginx:latest
   ```

2. 验证已打标签的镜像：

   ```bash
   docker images
   ```
