# 命令执行

在这一步中，你需要对 `alpine-container` 容器执行特定的命令。

## 任务

- 在名为 `alpine-container` 的容器内执行命令 `echo "Hello, World!"`。
- 确认命令在容器内成功输出结果。

## 要求

- 容器必须使用 `alpine` Docker 镜像。
- 容器的名称应为 `alpine-container`。
