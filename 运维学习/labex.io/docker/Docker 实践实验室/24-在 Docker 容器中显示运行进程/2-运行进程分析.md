# 运行进程分析

在这一步中，你将分析正在运行的进程，以识别任何异常或资源密集型任务。使用 `docker exec` 命令访问容器的 shell，并运行诸如 `top` 或 `ps` 等工具来进一步调查这些进程。

#### 示例

```bash
# 访问容器的 shell
$ docker exec -it t-rex-adventure /bin/sh

# 在容器 shell 中，分析正在运行的进程
top
```
