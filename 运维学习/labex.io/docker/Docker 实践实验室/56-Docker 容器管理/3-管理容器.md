# 管理容器

在此步骤中，你将学习如何启动、停止和移除 Docker 容器。

- 使用官方 nginx 镜像启动一个名为 "nginx" 的容器：

  ```shell
  docker run --name nginx -d -p 8080:80 nginx
  ```

- 停止正在运行的 "nginx" 容器：

  ```shell
  docker stop nginx
  ```

- 移除 "nginx" 容器：

  ```shell
  docker rm nginx
  ```
