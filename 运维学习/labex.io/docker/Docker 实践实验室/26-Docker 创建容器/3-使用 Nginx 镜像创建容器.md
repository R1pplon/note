# 使用 Nginx 镜像创建容器

在这一步中，你将通过使用 Nginx 镜像创建容器，进一步深入这个迷人的世界。使用以下魔法来创建 Nginx 容器：

```bash
docker run -d -p 80:80 --name my-nginx nginx
```

通过这条命令，名为 "my-nginx" 的 Nginx 容器将被召唤出来，使你能够利用 Nginx 的力量来实现自己的愿望。
