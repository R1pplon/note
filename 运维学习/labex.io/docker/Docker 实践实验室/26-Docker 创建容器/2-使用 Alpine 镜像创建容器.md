# 使用 Alpine 镜像创建容器

在这一步中，你将利用神奇的 Alpine 镜像的力量来创建你的容器。使用以下咒语来创建这个容器：

```bash
docker run -it --name alpine-container alpine /bin/sh
```

这条命令将施放魔法，创建一个名为 "alpine-container" 的交互式 Alpine 容器。随后，你将获得进入其中所包含的神秘领域的权限。
