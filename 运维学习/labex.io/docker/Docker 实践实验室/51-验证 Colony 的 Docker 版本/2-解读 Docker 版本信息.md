# 解读 Docker 版本信息

## 任务

- 解读 Docker 版本信息，了解客户端（Client）和服务器端（Server）的版本。
- 记录有关 Docker 客户端和服务器版本详情。

## 要求

- 使用与步骤 1 相同的默认工作目录和终端环境。
- 分析显示 docker version 的输出，以提取客户端和服务器的版本详情。

## 示例

执行命令后，观察并从输出中提取客户端和服务器的版本详情。
