# 获取魔法师的知识

在这最后一步中，魔法师揭示了神庙密室中隐藏的知识。你将学习如何备份和恢复存储在 Docker 卷中的数据。

1. 在终端中运行以下命令，创建卷数据的备份：

```bash
docker run --rm -v my_volume:/source -v $(pwd):/backup alpine tar cvf /backup/backup.tar /source
```

2. 检查备份文件 `backup.tar` 是否已成功创建在当前目录中。
