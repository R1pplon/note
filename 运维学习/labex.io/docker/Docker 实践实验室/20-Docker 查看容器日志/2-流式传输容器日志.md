# 流式传输容器日志

在这一步中，你将学习如何实时流式传输运行中容器的日志。

1. 从 `magical-nginx` 容器流式传输日志：

   ```bash
   docker logs -f magical-nginx
   ```

2. 在保持日志流打开的同时，添加一个 Web 服务映射。如果服务映射成功，环境将自动在另一个标签页中访问默认的 Nginx 网页，以生成日志条目。

   ```bash
   curl http://localhost
   ```

3. 观察实时日志条目在日志流中显示。
