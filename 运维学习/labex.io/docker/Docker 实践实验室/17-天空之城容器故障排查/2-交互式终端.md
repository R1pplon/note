# 交互式终端

在这一步骤中，你将练习在 Docker 容器中使用交互式终端。

## 任务

- 在一个运行中的 Docker 容器中启动交互式终端。
- 使用交互式终端在容器内创建一个名为 `testfile.txt` 的新文件。

## 要求

- 本任务请使用 `alpine` 镜像。
- 容器名称为 `alpine_demo`。
- 文件 `testfile.txt` 应创建在容器的根目录下。
