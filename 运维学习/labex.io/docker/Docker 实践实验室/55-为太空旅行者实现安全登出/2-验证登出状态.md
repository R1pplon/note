# 验证登出状态

## 任务

- 确认 Docker 镜像仓库的登出已反映在系统中。
- 确保登出后无法未经授权访问 Docker 镜像仓库。

## 要求

- 你必须仍在默认工作目录：`~/project` 中操作。
- 检查 Docker 配置以确认登出状态。

## 示例

查看我们修改过的 Docker 守护进程的配置文件。

```json
{
  "auths": {}
}
```
