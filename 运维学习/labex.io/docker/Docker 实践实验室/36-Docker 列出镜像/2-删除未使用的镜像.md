# 删除未使用的镜像

在这一步中，你将通过删除可能隐藏证据的未使用镜像来清理 Docker 环境。执行以下命令以清除多余的镜像：

```bash
# 删除特定的 Docker 镜像

# 删除所有未使用的镜像
```

- `docker image rm <image_name>` 命令会从系统中删除指定的 Docker 镜像。
- `docker image prune` 命令会删除所有未使用的镜像以释放存储空间。
