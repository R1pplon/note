# 探索 Docker 镜像

在这一步中，你将通过深入了解可能隐藏犯罪头目秘密活动的 Docker 镜像来展开调查。使用以下命令列出所有可用的 Docker 镜像并检查它们的详细信息：

```bash
# 列出所有 Docker 镜像

# 显示特定镜像的详细信息
```

- `docker images` 命令会检索你系统上所有可用的 Docker 镜像列表。
- `docker image inspect <image_name>` 命令会提供特定镜像的详细信息。
