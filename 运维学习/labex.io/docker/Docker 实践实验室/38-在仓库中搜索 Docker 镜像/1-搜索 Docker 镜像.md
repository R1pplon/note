# 搜索 Docker 镜像

在这一步中，你将学习如何在存储库中搜索 Docker 镜像。使用 `docker search` 命令查找与你的建筑项目相关的可用镜像。

#### 示例

```bash
docker search nginx
```

说明：

- `docker search`：用于在存储库中搜索 Docker 镜像的命令。
- `nginx`：搜索关键词的示例，可以替换为你所需的关键词。
