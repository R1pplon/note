# 过滤搜索结果

在这一步中，你将学习如何根据特定条件（如星级评分和官方状态）过滤搜索结果，以缩小可用的 Docker 镜像范围。

#### 示例

```bash
docker search --filter=stars=3 nginx
```

说明：

- `docker search --filter=stars=3`：根据星级评分过滤搜索结果的命令。
- `nginx`：搜索关键词的示例，可以替换为你所需的关键词。
