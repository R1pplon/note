# 查看脚本内容

作为最后一步，让我们查看脚本文件的内容以确认一切无误。我们可以使用 `cat` 命令来完成，它会在终端中显示文件的内容。

在终端中输入以下命令并按回车：

```bash
cat hello.sh
```

你应该会看到脚本的内容被显示出来：

```bash
#!/bin/bash
echo 'Hello, World!'
```

在处理脚本时养成这个习惯非常好 —— 永远要复核你的工作！
