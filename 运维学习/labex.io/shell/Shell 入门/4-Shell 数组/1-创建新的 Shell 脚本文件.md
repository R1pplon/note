# 创建新的 Shell 脚本文件

首先，我们创建一个新的 Shell 脚本文件，用于编写数组操作代码。

1. 在 WebIDE 中打开终端。
2. 切换到项目目录：

   ```bash
   cd ~/project
   ```

3. 使用 `touch` 命令创建一个名为 `arrays.sh` 的新文件：

   ```bash
   touch arrays.sh
   ```

4. 在 WebIDE 中打开 `arrays.sh` 文件。
