# 总结

在本实验中，你成功安装并验证了 Nginx Web 服务器。你学习了如何：

1. 使用 `apt` **安装** Nginx 软件包。
2. 使用 `service` **管理**后台服务。
3. 使用 `ss` **验证**网络监听端口。
4. 使用 `curl` **测试**服务器响应。

这些是任何 Linux 系统管理员的基本技能。现在，你已经拥有了一个可供配置的 Web 服务器。
