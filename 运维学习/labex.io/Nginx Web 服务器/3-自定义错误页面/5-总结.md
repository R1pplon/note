# 总结

在本实验中，你配置了 Nginx 以在发生错误时提供更好的用户体验。你学习了如何：

1. **设计**自定义 HTML 错误页面。
2. **配置** Nginx 服务器块中的 `error_page` 指令。
3. **使用** `internal` 保护错误页面，防止直接访问。
4. **使用** `curl` 验证配置。

自定义错误页面是一种简单但专业的做法，它能让任何 Web 服务器的实现变得更加稳健且对用户友好。
